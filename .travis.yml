language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t alexiastandish/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true alexiastandish/docker-react npm run test
